# proxy-chat-api

## create .env
$ cp env.example .env
$ vim .env

## run fast api server
$ docker-compose up -d

## stop fast api server
docker compose down

## run traefik
$ docker compose -f traefik.yml up -d

## stop traefik
$ docker compose -f traefik.yml down
